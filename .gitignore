# ローカル実行時の出力 (GitHub Pages では配信しない)
comments_local/

# Secrets (YT_COOKIES から実行時に生成。絶対にコミットしない)
cookies.txt

# yt-dlp が残す一時ファイル (通常は一時ディレクトリに出るが手動実行の保険)
*.live_chat.json

# Python
__pycache__/
*.pyc
.venv/
